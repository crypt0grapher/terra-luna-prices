import { useContext, useState } from "react";
import TradeContext from "../hooks/tradeContext";

const TradeSetupPanel = () => {
  const { tradeParams, setTradeParams } = useContext(TradeContext);
  return <div/>
}
export default TradeSetupPanel;
